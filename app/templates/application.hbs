<div class="container">
  <div class="row" >
    <nav class="navbar navbar-expand navbar-light bg-light mb-5 w-100">
      {{#link-to "index" class="navbar-brand"}}
        <img src="/logo-small.png">
        {{/link-to}}
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav">
          <li class="nav-item">
            {{#link-to "index" class="nav-link"}}
              Home
              {{/link-to}}
          </li>
          <li class="nav-item">
            {{#link-to "feed" class="nav-link"}}
              Feed
              {{/link-to}}
          </li>
        </ul>
        <ul class="navbar-nav float-right">
          <li class="nav-item">
            {{#link-to "profile" session.currentUser class="nav-link"}}
              {{fa-icon "user-circle-o"}}
              {{session.currentUser.userName}}
            {{/link-to}}
          </li>
        </ul>
      </div>
  </nav>
  </div>
 {{outlet}}
</div>